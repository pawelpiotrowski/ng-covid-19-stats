import { TransformerClass } from './transformer-class';

describe('TransformerClass', () => {
  it('should create an instance', () => {
    expect(new TransformerClass('globalStatsCards')).toBeTruthy();
  });
});
